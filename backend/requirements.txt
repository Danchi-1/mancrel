# ── Core API framework ────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── LLM / AI reply generation ─────────────────────────────────────────────────
# openai SDK v1.x — v2 has breaking URL construction for non-OpenAI providers
openai>=1.30.0,<2.0.0

# ── Message classifier (SetFit — CPU-safe, no GPU needed) ────────────────────
# Local ML dependencies (setfit, sentence-transformers, datasets, pandas) 
# have been removed to prevent Out-Of-Memory crashes on the Render free tier.
# Classification now relies entirely on the OpenRouter API.

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
aiosqlite>=0.20.0

# ── Authentication ──────────────────────────────────────────────────────────
# passlib is abandoned and broken with bcrypt 4.x — using bcrypt directly instead
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0    # loads .env file
pydantic-settings>=2.0.0
pydantic[email]>=2.10.3
twilio==9.10.9
httpx>=0.27.0           # async HTTP client for Meta Graph API calls
google-auth==2.53.0
requests>=2.31.0
